Dear <%= @abstract.attendee.first_name %>,

You or a site admin has updated your abstract.  Please review the information below:

<% %w{name email}.each do |w| -%><%= "#{w.capitalize}: #{@abstract.attendee.send(w)}
" if @abstract.attendee.send(w) && @abstract.attendee.send(w) != '' %><% end %>
<% %w{title authors affiliations body}.each do |w| -%><%= "#{w.capitalize}: #{@abstract.send(w)}
" if @abstract.send(w) && @abstract.send(w) != '' %><% end %>
Keywords: <%= @abstract.keyword_list %>

You may edit your abstract until May 6 12PM PDT using the following form: <%= link_to 'https://bic.ucop.edu/2013/abstracts/submit', 'https://bic.ucop.edu/2013/abstracts/submit' %>. Changes made after May 13 will not be reflected in the electronic program handed out on flash drives at the event but will be reflected on the website. 

Please contact <%= mail_to 'bic@ucop.edu' %> if you have any questions.

Thanks